# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local Docker Compose overrides (use .env.example as a template)
.env

# Local tooling / agent state
.claude/
.idea/
.vscode/

# Real HR / employee spreadsheets — never commit PII (import at runtime instead)
*.xlsx
*.xls
