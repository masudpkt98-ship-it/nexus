# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local Docker Compose overrides (use .env.example as a template)
.env

# Local tooling / agent state
.claude/
.idea/
.vscode/

# Real HR / employee & competency source data — never commit PII / proprietary
# content (import at runtime instead)
*.xlsx
*.xls
*.pdf

# dropped screenshots / scratch images at repo root (keep the brand asset)
/*.png
!/Nexus.png

# temp scratch scripts
nexus-app/_*

# local dev launcher (personal, not shared)
/start-dev.sh

# Source spec documents (not committed)
*.docx
